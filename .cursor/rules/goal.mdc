---
alwaysApply: true
---


I want to make a Docker image that plays Windows games on my very particular setup

The image should mount /mnt/shared from the host

The host uses gfxstream to render Vulkan and only supports Vulkan 1.1.305 and OpenGL Zink via gfxstream

```/usr/share/vulkan/icd.d/gfxstream_vk_icd.json
{
    "ICD": {
        "api_version": "1.1.305",
        "library_path": "libvulkan_gfxstream.so"
    },
    "file_format_version": "1.0.0"
}
```
```enable_gstream
#!/bin/bash
cat << EOF > /home/droid/weston.env
MESA_LOADER_DRIVER_OVERRIDE=zink
VK_ICD_FILENAMES=/usr/share/vulkan/icd.d/gfxstream_vk_icd.json
MESA_VK_WSI_DEBUG=sw,linear
XWAYLAND_NO_GLAMOR=1
EOF
sudo systemd-run --collect -E XDG_SESSION_TYPE=wayland --uid=1000 -p PAMName=login -p TTYPath=/dev/tty7 sleep 1d
(sleep 3s; systemctl --user start weston)& disown
export DISPLAY=:0
export MESA_LOADER_DRIVER_OVERRIDE=zink

unset LIBGL_ALWAYS_SOFTWARE

export VK_ICD_FILENAMES=/usr/share/vulkan/icd.d/gfxstream_vk_icd.json

# remove them after TILING_OPTIMAL-based swap chain is used.
export MESA_VK_WSI_DEBUG=sw,linear
export XWAYLAND_NO_GLAMOR=1
export LIBGL_KOPPER_DRI2=1
```
`/usr/lib/aarch64-linux-gnu/libvulkan_gfxstream.so`

Decide how best to ensure Vulkan and OpenGL from the host work in the Docker instance

Consider installing `https://github.com/pythonlover02/DXVK-Sarek/releases/download/v1.11.0/dxvk-sarek-async-v1.11.0.tar.gz` or other versions into the wine prefix, as dxvk-sarek supports using vulkan versions below 1.3

Install `https://github.com/Ilan12346-maya/XinputBridge/releases/download/1.35/winefiles_1.35.zip` to the wine prefix and `https://github.com/nanofuxion/XinputBridge/releases/download/v1.0.1/`udp-proxy-package.zip to the container image as per `https://github.com/nanofuxion/XinputBridge/blob/main/PROXY_README.md`

Validate file structures in archive files to ensure they are extracted correctly, and review GitHub repo readme/md files to ensure correct usage

the host is an arm64 vm and it would probably be best to use a wine version with wow64 like `https://github.com/Kron4ek/Wine-Builds/releases/download/10.0/wine-10.0-amd64-wow64.tar.xz` to reduce prefix managemnt complexity between 32bit and 64bit 

the current project already has a docker image that uses box64 we just need to build on that

The goal is to make a Docker image that I can just start, then attach and play a game